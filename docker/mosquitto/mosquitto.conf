# Local dev broker only — no auth, no TLS. See openspec/changes/kafka-mqtt-bridge/design.md
# ("Node authentication" Open Question) for the production extension points.
listener 1883
allow_anonymous true
persistence false
